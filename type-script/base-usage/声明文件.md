## 声明文件的作用
```
 1： 当使用的第三方的库采用的是ts的语言，那么就需要引入ts的文件声明。以此来获得代码片段的提示，接口提示等功能。
 声明语句： 如果需要ts对新的（引入的第三方包）语法进行检查，需要加载对应的类型的说明代码。
 declare var jQuery: (seletor: string) => any;
 声明文件： 把声明语句放到一个单独的文件（jQuery.d.ts）中。ts会自动解析项目中所有的声明文件。
 下载声明文件： npm i @types/jquery --save-dev

```