```js
1: const obj = {};
 const  state = reactive(obj);
const state1 = reactive(obj);
issue :
1: state的变化是否会影响obj的变化；
2： state1的变化会影响obj的变化吗？
ref 
reactive 对于原来对象的影响

对于每个api的作用 ： 是否会影响ui的变化；是否会影响原始数据 ；
```

